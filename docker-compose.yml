services:
  db:
    image: postgres:16
    restart: unless-stopped
    ports:
      - "5434:5432"
    environment:
      POSTGRES_USER: accrue
      POSTGRES_PASSWORD: accrue
      POSTGRES_DB: accrue
    volumes:
      - accrue_db:/var/lib/postgresql/data

volumes:
  accrue_db:
